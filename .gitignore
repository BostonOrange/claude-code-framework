# OS
.DS_Store
Thumbs.db

# Editors
.idea/
.vscode/
*.swp
*.swo
*~

# Dependencies (if any)
node_modules/

# Environment
.env
.env.local
.env.*.local

# Build artifacts (top-level only — `skills/build/` is the build skill, not output)
/dist/
/build/
*.pyc
__pycache__/
.sfdx

# Temp files
/tmp/
*.tmp

# Local hook state (per-machine, not project content)
.claude/state/
