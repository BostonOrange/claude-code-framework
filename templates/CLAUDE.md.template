# CLAUDE.md — Project Brain

## Project Overview

{{PROJECT_DESCRIPTION}}

## Tech Stack

{{TECH_STACK_TABLE}}

## System Structure

```
{{CODE_STRUCTURE}}
```

## Quick Start

| You are a... | Start with | Purpose |
|--------------|------------|---------|
| Architect/PM | `/draft-story` | Requirements → implementation-ready tickets |
| Architect/PM | `/refine-story TICKET-xxx` | Improve existing ticket + gap analysis |
| Architect/PM | `/team architecture` | Architecture review before implementation |
| Developer | `/develop TICKET-xxx` | Full dev cycle: implement, validate, PR |
| Developer | `/validate TICKET-xxx` | Validate code against project standards |
| Developer | `/error-analyze` | Triage production errors, create tickets |
| Reviewer | `/team review` | Parallel code + security + UX review |
| DevOps | `/team release` | Release readiness: security + infra + perf |
| Any | `/factory TICKET-xxx` | End-to-end: readiness → develop → validate → PR → deploy |
| Any | `/improve` | Evolve this file and .claude/ config from project state |

## Commands Available

| Command | Purpose |
|---------|---------|
| `/quick-test` | Run tests on changed files only |
| `/lint-fix` | Auto-fix lint issues in changed files |
| `/check-types` | Run type checker |
| `/branch-status` | Show branch diff, PR, and CI status |
| `/changelog` | Generate changelog from commits since last tag |
| `/dep-check` | Check for outdated or vulnerable dependencies |

## Skills Available

| Skill | Purpose |
|-------|---------|
| `/develop` | Full dev cycle: fetch ticket, implement, validate, PR |
| `/validate` | Validate code against project standards |
| `/factory` | End-to-end pipeline: readiness → develop → validate → PR → deploy |
| `/check-readiness` | Validate ticket is implementation-ready |
| `/draft-story` | Create tickets from requirements or design docs |
| `/refine-story` | Improve existing tickets with gap analysis |
| `/error-analyze` | Triage production errors |
| `/deploy` | Orchestrate deployments |
| `/merge-resolve` | AI-powered merge conflict resolution |
| `/team` | Spawn agent teams for parallel analysis |
| `/plan` | Multi-agent planning — planner-coordinator + planning specialists; produces `.claude/state/plan-<branch>.md` |
| `/build` | Multi-agent implementation — build-coordinator + build specialists; executes the plan in sequenced phases |
| `/iterative-review` | Plan → code → review → re-code loop with persistent state across iterations |
| `/setup` | First-time onboarding — inventories repo, runs 17-layer detection with tradeoff-explained options, applies confirmed proposal (uses `project-setup-detector` + `project-setup-applier`) |
| `/impact` | On-demand cascade analysis for a symbol or file (uses `impact-analyzer`); produces a confidence-scored "what would break if I change this" report |
| `/index` | Build/refresh vector index at `.claude/state/codebase.db` (opt-in for codebases >50k LOC) |
| `/search` | Semantic search across the indexed codebase — answers "where do we handle X?" queries |
| `/improve` | Self-improvement — update CLAUDE.md, rules, settings from project state (also refreshes `.claude/state/architecture.md`) |
| `/ai-update` | Branch + PR for AI process changes |
| `/add-reference` | Add/update knowledge reference docs |
| `/update-tracker` | Push story docs to work item tracker |
| `/mock-endpoint` | Mock external API integrations |
| `/fetch-docs` | Fetch and persist external documentation |
| `/scaffold-design-system` | Bootstrap design tokens, base components, and CLAUDE.md design conventions |

## Agent Teams

Spawn pre-configured teams of specialized agents for parallel analysis:

| Team | Command | Agents | When to Use |
|------|---------|--------|-------------|
| **Review** | `/team review` | code-reviewer, security-auditor, ui-ux-reviewer | Before merging PRs |
| **Review-deep** | `/team review-deep` | code-reviewer, security-auditor, code-smell-reviewer, dry-reviewer, purity-reviewer, complexity-reviewer | Before merging non-trivial PRs |
| **Architecture** | `/team architecture` | architect, api-designer, database-architect | Design phase, health checks |
| **Release** | `/team release` | security-auditor, devops-engineer, performance-optimizer | Before releases |
| **Quality** | `/team quality` | code-reviewer, test-writer, performance-optimizer | After implementation |
| **Quality-deep** | `/team quality-deep` | code-smell-reviewer, dry-reviewer, purity-reviewer, complexity-reviewer | Targeted code-quality sweep |
| **Design** | `/team design` | ui-ux-reviewer, performance-optimizer, refactor-advisor | UI/UX consistency, design system compliance |
| **Documentation** | `/team documentation` | documentation-writer, api-designer | After API changes |
| **Full** | `/team full` | All 16 reviewer/implementation agents | Major milestones |
| **Custom** | `/team custom a b c` | Any combination | Targeted analysis |

## Agents Available

### Analysis Agents (read-only)

| Agent | Purpose | Model |
|-------|---------|-------|
| `architect` | System design review, architecture patterns, scalability | opus |
| `code-reviewer` | Review diffs for bugs, security, performance, design principles, code smells, conventions (broad sweep) | opus |
| `code-smell-reviewer` | Code smells specialist: long methods, magic numbers, primitive obsession, data clumps, feature envy, dead code (cites `code-smells` rule) | opus |
| `dry-reviewer` | Duplication specialist: 3+ repeated logic, structural patterns (cites `dry` rule) | opus |
| `purity-reviewer` | Pure-function specialist: side effects, query/command separation, hidden state, SRP (cites `purity` rule) | opus |
| `complexity-reviewer` | Complexity specialist: function length, cyclomatic complexity, nesting, parameter count (cites `complexity` rule) | opus |
| `frontend-architecture-reviewer` | Frontend structure: composition, state, hooks, data flow, render-perf architecture (cites `frontend-architecture` rule) | opus |
| `architecture-reviewer` | Layering: dependency direction, cross-module reach, circular deps, god modules (cites `architecture-layering` rule) | opus |
| `api-layering-reviewer` | API structure: controller/service/repo separation, validation placement, error contract (cites `api-layering` rule) | opus |
| `crypto-reviewer` | OWASP A02: weak hashes, password storage, RNG, encryption modes/IV, JWT, TLS, key derivation (cites `crypto` rule) | opus |
| `solid-reviewer` | OCP/LSP/ISP/DIP (cites `solid` rule); SRP is purity-reviewer's domain | opus |
| `concurrency-reviewer` | Race conditions, TOCTOU, async/lock discipline, mutable shared state, background workers, channels (cites `concurrency` rule) | opus |
| `observability-reviewer` | OWASP A09: structured logging, log levels, metrics, tracing, audit logs, alerting, correlation (cites `observability` rule) | opus |
| `supply-chain-reviewer` | OWASP A06+A08: lockfiles, version pinning, CVE reachability, signing, dev/prod separation, CI pipeline integrity (cites `supply-chain` rule) | opus |
| `security-auditor` | OWASP audit, credentials, dependencies, compliance | opus |
| `refactor-advisor` | Duplication, complexity, extraction opportunities (broader than `dry-reviewer`) | opus |
| `devops-engineer` | CI/CD, containers, infrastructure, monitoring | opus |
| `ui-ux-reviewer` | Accessibility, design consistency, responsiveness | opus |
| `performance-optimizer` | Bundle size, queries, rendering, caching, memory | opus |
| `api-designer` | Endpoint design, schemas, versioning, DX | opus |
| `database-architect` | Schema, normalization, indexes, migration safety | opus |

### Planning Agents (read-only — used by `/plan`)

| Agent | Purpose | Model |
|-------|---------|-------|
| `requirements-clarifier` | Hunts ambiguity in story before planning starts (open questions, undefined terms, missing AC) | opus |
| `scope-decomposer` | Breaks story into atomic, sequenced steps with parallelism groups and dependencies | opus |
| `risk-assessor` | Identifies rollback paths, blast radius, breaking-change and migration risk; proposes mitigations | opus |
| `test-strategy-planner` | Decides what tests at what level (unit/integration/e2e/contract) per planned step | opus |

### Implementation Agents (can modify code)

| Agent | Purpose | Model |
|-------|---------|-------|
| `scaffold-implementer` | Build phase 1: file structure, types, signatures, stubs — no logic | opus |
| `happy-path-implementer` | Build phase 2: core successful flow logic | opus |
| `edge-case-implementer` | Build phase 3: input validation, error handling, edge data (binds `error-handling`, `auth-security`, `data-protection`) | opus |
| `refactor-pass-implementer` | Build phase 6 (final): actively applies `code-smells`/`dry`/`purity`/`complexity` rules; preempts review findings | opus |
| `test-writer` | Generate tests following project conventions and factories | opus |
| `documentation-writer` | API docs, READMEs, architecture docs, setup guides | opus |

### Meta Agents (orchestrate other agents)

| Agent | Purpose | Model |
|-------|---------|-------|
| `framework-improver-detector` | Self-improvement (read-only) — scans, builds /setup-aware skip-list, writes proposal (invoked by `/improve` Phase 1) | opus |
| `framework-improver-applier` | Self-improvement (write) — re-validates skip-list, applies improvements with backup + audit log (invoked by `/improve` Phase 3) | opus |
| `planner-coordinator` | Orchestrates planning specialists; classifies scope; synthesizes one plan (invoked by `/plan`) | opus |
| `build-coordinator` | Orchestrates build phases sequentially: scaffold → happy-path → edge-case → tests → docs → refactor (invoked by `/build`) | opus |
| `review-coordinator` | Synthesizes parallel reviewer output — dedupes, filters, classifies risk tier, persists state across iterations (invoked by `/iterative-review`) | opus |
| `project-setup-detector` | First-time onboarding (read-only) — inventories repo, runs 17-layer detection, writes proposal (invoked by `/setup` Phase 1) | opus |
| `project-setup-applier` | First-time onboarding (write) — reads confirmed proposal, validates allowlist, snapshots, applies substitutions (invoked by `/setup` Phase 4) | opus |
| `impact-analyzer` | On-demand cascade analysis for a symbol/file — greps callers, classifies (direct/type/test), scores confidence (invoked by `/impact`) | opus |

## Work Item Tracker

{{TRACKER_CONFIG}}

## Story Tickets & Refinement

### Workflow

```
Architect/PM drafts story → /team architecture validates design
    → /check-readiness gates ticket
    → Developer runs /factory or /develop
    → /team review before merge
    → /team release before deploy
    → /improve evolves the framework
```

### Story Folder Structure

Create `docs/stories/TICKET-{xxx}/` with:

| File | Purpose |
|------|---------|
| `story.md` | Full story with ACs, technical spec |
| `solutions-components.md` | Components changed/created |
| `how-to-test.md` | Testing instructions |
| `manual-steps.md` | Pre/post deployment steps (if needed) |

### Writing Style

Write for a **technical audience**. Be direct, no filler. Use identifiers, not display names. Tables over paragraphs.

## Design System

> If no design system is configured, this section may be empty. Run `/scaffold-design-system` to bootstrap one, or `/improve` to auto-detect and document an existing one.

### Color Usage

{{DESIGN_COLOR_RULES}}

### Component Imports

{{DESIGN_COMPONENT_IMPORTS}}

### Icon Usage

{{DESIGN_ICON_USAGE}}

### Card & Layout Patterns

{{DESIGN_CARD_PATTERNS}}

### Dark Mode

{{DESIGN_DARK_MODE}}

## Architectural Awareness

For module map, dependency direction (Mermaid), impact hotspots, cross-cutting concerns, common patterns, architectural invariants, and recently-churned files, see **`.claude/state/architecture.md`** — auto-generated by `/improve` and refreshed every session. Agents that benefit (architect, architecture-reviewer, refactor-advisor, dry-reviewer, impact-analyzer, code-reviewer) read it before acting.

If the file is missing, run `/improve` to generate it. If the file is stale (timestamp older than the most recent commit), run `/improve` to refresh — that pipeline regenerates `architecture.md` as part of its standard pass.

For precise on-demand impact analysis of a specific symbol or file, run **`/impact <symbol-or-file>`** — produces a targeted "what would break if I change this" report at `.claude/state/impact-<hash>.md`.

For semantic search across a large codebase ("where do we handle invoice number generation?"), run **`/search "natural language query"`** — uses the vector index at `.claude/state/codebase.db` (built by `/index`). The index is opt-in; small projects don't need it.

## Coding Conventions

### Code Standards

{{CODING_STANDARDS}}

### Error Handling

{{ERROR_HANDLING_PATTERN}}

### Testing Strategy

{{TESTING_STRATEGY}}

## Common Development Commands

### Code Formatting & Linting

{{FORMAT_COMMAND}}

### Type Checking

```bash
{{TYPE_CHECK_COMMAND}}
```

### Testing

{{TEST_COMMAND}}

### Deployment

{{DEPLOY_VALIDATE_COMMAND}}

## Version Control

### Branching Strategy

- **`{{BASE_BRANCH}}`** — primary integration branch
- **Feature branches** — `TICKET-xxx` format, branched from `{{BASE_BRANCH}}`
- **Release branches** — cut from `{{BASE_BRANCH}}` when ready

### Pull Request Guidelines

- **Base branch**: `{{BASE_BRANCH}}`
- **PR title format**: `{Ticket-ID}: {Story Title}`
- **Assignee**: Always assign to yourself (`--assignee @me`)

### Git Rules

- Never force push to `{{BASE_BRANCH}}`
- Stage specific files — never `git add -A` or `git add .`
- No commits with failing tests
- Only maintainer can approve merges to `{{BASE_BRANCH}}`

## Security

### Data Protection
- **No real PII in git** — never commit database files, uploaded documents, or CSVs with real personal data. Use synthetic test data only.
- **No credentials on disk** — `.env` should have placeholders only. Real secrets belong in a vault or CI secrets. Rotate immediately if exposed. Never put secrets in client-exposed env vars (`NEXT_PUBLIC_*`, `VITE_*`, `REACT_APP_*`).
- **No PII in logs** — use opaque IDs, not names/emails/personal numbers. Configure structured logger redaction.
- **Flag third-party PII sharing** — any code sending personal data to external APIs (AI providers, analytics) must be reviewed for DPA compliance.

### Authentication & Authorization
- **Auth must fail closed** — if auth configuration is missing, DENY access. Never default to open. Add a production startup guard.
- **No insecure secret defaults** — session secrets must not have hardcoded fallback values. Crash if not configured.
- **CSRF protection** on all state-changing form endpoints (POST/PUT/DELETE). API-only apps with Bearer tokens are exempt.
- **Enforce roles in routes** — if roles exist in the data model, route handlers must check them. Roles in the DB but not in code = no access control.
- **Identity from session only** — never accept reviewer/approver/user identity from request body or query params.
- **Validate redirects** — redirect targets must be relative paths. Never redirect to user-supplied absolute URLs.
- **Session cookies**: `Secure=true`, `HttpOnly=true`, `SameSite=Strict` (or `Lax`) in production.

### General
- No secrets in code — use environment variables and `.env`
- Validate all input at system boundaries with a schema library (Zod, Pydantic, etc.)
- Review dependency updates for security advisories
- Keep `.env` and credential files in `.gitignore`
- Never set `ignoreBuildErrors: true` or equivalent — type errors can hide security bugs
- Rate limit authentication, AI, and file upload endpoints
- Never use `Access-Control-Allow-Origin: *` with credentials — use an explicit origin allow-list
- Validate outbound URLs from user input — block private IP ranges to prevent SSRF
- Sanitize uploaded filenames (strip path separators, reject `..`) to prevent path traversal

## Factory Mode

When invoked with `/factory TICKET-{id}`:
- All human gates use auto-defaults
- Ticket readiness checked automatically (`/check-readiness`)
- Code standards validated locally — **deployment handled by CI**
- PR gets `factory` label → triggers CI workflows
- On halt: notify team, do not wait for user

### Pipeline

```
/draft-story (architect resolves questions)
    → /team architecture (validate design)
    → Ticket "Ready for Sprint"
    ↓
/factory TICKET-xxx
  ├─ /check-readiness
  ├─ Create worktree + branch
  ├─ Scaffold + implement
  ├─ /validate (code standards only)
  └─ Commit + PR (factory label) → CI deploys → reviewer tests → merge → deploy
```

## Self-Improvement (always active)

**Before ending any session where project files were modified**, spawn `/improve` in the background (it orchestrates `framework-improver-detector` → `framework-improver-applier`). This keeps CLAUDE.md, rules, and settings in sync with the actual project state — not just after `/develop` or `/factory`, but after any work session including manual coding, refactoring, config changes, or dependency updates. The applier respects the `/setup`-owned skip-list, so onboarding decisions are never overwritten.

The improver:
- Fills unfilled `{{...}}` placeholders from project analysis
- Updates `.claude/rules/` patterns to match actual file structure
- Updates this file (CLAUDE.md) with discovered conventions and patterns
- Tunes agent tools for project needs
- Logs all changes to `docs/ai-improvements.md` (append-only)

**This is not optional.** If files changed during the session, run the improver before wrapping up. The project documentation must always reflect the current state.

For manual improvement: run `/improve` directly.
After improvement: run `/ai-update` to commit changes as a tracked PR.

## MCP Servers

Context7 (`.mcp.json`) provides live, version-specific library documentation. Several skills and agents use it proactively — they call `resolve-library-id` then `get-library-docs` before making implementation decisions, so recommendations stay current with the latest API surfaces. No action needed from you; it works automatically during `/develop`, `/draft-story`, `/refine-story`, `/mock-endpoint`, and when agents like `architect`, `api-designer`, `test-writer`, `documentation-writer`, or `performance-optimizer` are active.

To trigger it manually, add "use context7" to any prompt about a specific library.

## Integrations

{{INTEGRATIONS}}
